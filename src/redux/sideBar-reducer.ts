import {ActionType, PostType, SideBarType} from "./state";

const sideBarReducer = (state: SideBarType, action: ActionType) => {
    switch (action.type) {
        default:
            return state
    }
}

export default sideBarReducer;